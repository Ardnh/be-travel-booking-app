info:
  name: Delete Schedule
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{base_url}}/api/v1/schedules/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5