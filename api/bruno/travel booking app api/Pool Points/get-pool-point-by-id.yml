info:
  name: Get Pool Point By ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/v1/pool-points/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5