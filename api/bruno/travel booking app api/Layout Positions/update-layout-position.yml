info:
  name: Update Layout Position
  type: http
  seq: 5

http:
  method: PUT
  url: "{{base_url}}/api/v1/layout-positions/:id"
  body:
    type: json
    data: |-
      {
        "label": "B1",
        "row": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5