info:
  name: Get User Roles By User ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/v1/users/:userId/user-roles"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5