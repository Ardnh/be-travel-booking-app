info:
  name: Get Layout Positions By Layout ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/v1/layouts/:layoutId/layout-positions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5