info:
  name: Get Schedules By Vendor ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/vendors/:vendorId/schedules"
  params:
    - name: vendorId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
