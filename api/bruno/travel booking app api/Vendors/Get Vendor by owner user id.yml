info:
  name: Get Vendor by owner user id
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/vendors/owner"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
