info:
  name: Get All Vendors
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/v1/vendors"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5