info:
  name: Update Service Type
  type: http
  seq: 4

http:
  method: PUT
  url: "{{base_url}}/api/v1/service-types/:id"
  body:
    type: json
    data: |-
      {
        "name": "Updated Service Type",
        "description": "Updated description"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5