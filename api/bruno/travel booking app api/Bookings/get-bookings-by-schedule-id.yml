info:
  name: Get Bookings By Schedule ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/v1/schedules/:scheduleId/bookings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5