info:
  name: Get Vendor By ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/vendors/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
