info:
  name: Get All Service Types
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/v1/service-types"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5