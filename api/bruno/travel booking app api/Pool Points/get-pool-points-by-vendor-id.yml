info:
  name: Get Pool Points By Vendor ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/v1/vendors/:vendorId/pool-points"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5