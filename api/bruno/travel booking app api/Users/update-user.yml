info:
  name: Update User
  type: http
  seq: 2

http:
  method: PUT
  url: "{{base_url}}/api/v1/users/:id"
  body:
    type: json
    data: |-
      {
        "name": "Updated User Name",
        "phone": "081234567890"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5