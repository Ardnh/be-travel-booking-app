info:
  name: Get All Layouts
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/v1/layouts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5