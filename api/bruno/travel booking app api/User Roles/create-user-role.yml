info:
  name: Create User Role
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/api/v1/user-roles"
  body:
    type: json
    data: |-
      {
        "user_id": "uuid-here",
        "role": "admin",
        "vendor_id": "uuid-here",
        "pool_id": "uuid-here"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5