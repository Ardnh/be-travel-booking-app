info:
  name: Create Service Type
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/api/v1/service-types"
  body:
    type: json
    data: |-
      {
        "name": "Travel Service",
        "description": "Description here"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5