info:
  name: Delete Booking
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{base_url}}/api/v1/bookings/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5