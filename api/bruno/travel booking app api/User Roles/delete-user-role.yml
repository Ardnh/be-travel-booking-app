info:
  name: Delete User Role
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{base_url}}/api/v1/user-roles/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5