info:
  name: Delete User
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{base_url}}/api/v1/users/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5