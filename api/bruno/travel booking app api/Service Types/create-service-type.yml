info:
  name: Create Service Type
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/service-types"
  body:
    type: json
    data: |-
      {
        "name": "Door to door",
        "unique_code": "DOOR-TO-DOOR",
        "description": "Antar jemput langsung ke tempat",
        "need_chair": true,
        "need_pickup_address": true,
        "need_dropoff_address": true,
        "display_order": 1,
        "status": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
